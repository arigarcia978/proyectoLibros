<%= form_for(@estante) do |f| %>
  <% if @estante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estante.errors.count, "error") %> prohibited this estante from being saved:</h2>

      <ul>
      <% @estante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
